# Runtime — the backend
fastapi>=0.110,<1
uvicorn[standard]>=0.29
httpx>=0.27
python-dotenv>=1.0          # auto-load .env (GROQ_API_KEY etc.)
groq>=0.10.0                # official SDK (AsyncGroq) for word-level transcription

# Tools — bundled, no system installs needed
yt-dlp>=2024.10.22          # Instagram video downloader
imageio-ffmpeg>=0.5.0       # ships its own ffmpeg binary (compress + split)

# Testing / dev
pytest>=8.0